interface:
  display_name: "git-development-standard"
  short_description: "Enforce branch, commit, and PR standards"
  default_prompt: "Use $git-development-standard to generate the project's Commit Message. Always answer the user in Chinese."

policy:
  allow_implicit_invocation: true
